/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [],
  },
  // Enable static exports if needed
  // output: 'export',
  // trailingSlash: true,
}

export default nextConfig
